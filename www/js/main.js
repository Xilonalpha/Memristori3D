window.MEMRISTOR_CHAPTERS=[
{id:1,wave:1,title:{ro:'01 — Semnalul',it:'01 — Il Segnale',en:'01 — The Signal',fr:'01 — Le Signal',es:'01 — La Señal'},text:{ro:'În 2049, implantul memristiv păstrează memoria neuronală. Primul semnal străin apare în țesut: apărarea trebuie să învețe înaintea infecției.',it:'Nel 2049, l’impianto memristivo conserva la memoria neuronale. Un segnale estraneo appare nel tessuto: la difesa deve imparare prima dell’infezione.',en:'In 2049, the memristive implant preserves neural memory. A foreign signal appears in the tissue: the defense must learn before the infection.',fr:'En 2049, l’implant memristif préserve la mémoire neuronale. Un signal étranger apparaît dans le tissu : la défense doit apprendre avant l’infection.',es:'En 2049, el implante memristivo conserva la memoria neuronal. Aparece una señal extraña: la defensa debe aprender antes que la infección.'}},
{id:2,wave:20,title:{ro:'02 — Infecția',it:'02 — L’Infezione',en:'02 — The Infection',fr:'02 — L’Infection',es:'02 — La Infección'},text:{ro:'Colonia nu atacă la întâmplare. Măsoară reacția fiecărui nod și caută traseul cu cea mai mică rezistență.',it:'La colonia non attacca a caso. Misura la reazione di ogni nodo e cerca il percorso con la minore resistenza.',en:'The colony does not attack randomly. It measures each node and searches for the path of least resistance.',fr:'La colonie n’attaque pas au hasard. Elle mesure chaque nœud et cherche le chemin de moindre résistance.',es:'La colonia no ataca al azar. Mide cada nodo y busca el camino de menor resistencia.'}},
{id:3,wave:45,title:{ro:'03 — Răspunsul',it:'03 — La Risposta',en:'03 — The Response',fr:'03 — La Réponse',es:'03 — La Respuesta'},text:{ro:'Directorul adaptiv schimbă presiunea. Nu mai există o singură apărare corectă: combinația și poziționarea devin arme.',it:'Il direttore adattivo cambia la pressione. Non esiste più una sola difesa corretta: combinazione e posizione diventano armi.',en:'The adaptive director changes the pressure. There is no single correct defense: combination and positioning become weapons.',fr:'Le directeur adaptatif change la pression. Il n’existe plus une seule bonne défense : combinaison et placement deviennent des armes.',es:'El director adaptativo cambia la presión. Ya no existe una sola defensa correcta: combinación y posición son armas.'}},
{id:4,wave:70,title:{ro:'04 — Războiul adaptiv',it:'04 — La Guerra Adattiva',en:'04 — The Adaptive War',fr:'04 — La Guerre Adaptative',es:'04 — La Guerra Adaptativa'},text:{ro:'Patogenii au învățat să tragă, să se camufleze și să se vindece. Implantul începe să construiască tactici, nu doar turnuri.',it:'I patogeni hanno imparato a sparare, mimetizzarsi e guarire. L’impianto costruisce tattiche, non solo torri.',en:'Pathogens learned to shoot, cloak and heal. The implant now builds tactics, not just towers.',fr:'Les agents pathogènes ont appris à tirer, se camoufler et se soigner. L’implant construit des tactiques, pas seulement des tours.',es:'Los patógenos aprendieron a disparar, camuflarse y curarse. El implante construye tácticas, no solo torres.'}},
{id:5,wave:100,title:{ro:'05 — Arhiva Neagră',it:'05 — L’Archivio Nero',en:'05 — The Black Archive',fr:'05 — L’Archive Noire',es:'05 — El Archivo Negro'},text:{ro:'O memorie ascunsă indică faptul că infecția a fost proiectată să evolueze. Cineva a lăsat în urmă un protocol de supraviețuire.',it:'Una memoria nascosta indica che l’infezione è stata progettata per evolvere. Qualcuno ha lasciato un protocollo di sopravvivenza.',en:'A hidden memory suggests the infection was designed to evolve. Someone left behind a survival protocol.',fr:'Une mémoire cachée indique que l’infection a été conçue pour évoluer. Quelqu’un a laissé un protocole de survie.',es:'Una memoria oculta indica que la infección fue diseñada para evolucionar. Alguien dejó un protocolo de supervivencia.'}},
{id:6,wave:140,title:{ro:'06 — Contragreutatea',it:'06 — Il Contrappeso',en:'06 — Counterweight',fr:'06 — Le Contrepoids',es:'06 — El Contrapeso'},text:{ro:'Rețeaua intră în faza de asediu. Fiecare resursă trebuie economisită pentru ferestrele de vulnerabilitate și pentru contraatac.',it:'La rete entra nella fase d’assedio. Ogni risorsa va conservata per le finestre di vulnerabilità e il contrattacco.',en:'The network enters siege phase. Every resource must be saved for vulnerability windows and the counterattack.',fr:'Le réseau entre en phase de siège. Chaque ressource doit être conservée pour les fenêtres de vulnérabilité et la contre-attaque.',es:'La red entra en fase de asedio. Cada recurso debe guardarse para las ventanas de vulnerabilidad y el contraataque.'}},
{id:7,wave:175,title:{ro:'07 — Protocolul Omega',it:'07 — Protocollo Omega',en:'07 — Omega Protocol',fr:'07 — Protocole Omega',es:'07 — Protocolo Omega'},text:{ro:'Se detectează o conștiință patogenă. Analiza indică rezistență adaptivă, faze defensive și atacuri coordonate. La 200 începe confruntarea finală.',it:'Viene rilevata una coscienza patogena. L’analisi indica resistenza adattiva, fasi difensive e attacchi coordinati. A 200 inizia lo scontro finale.',en:'A pathogen consciousness is detected. Analysis shows adaptive resistance, defensive phases and coordinated attacks. The final battle begins at 200.',fr:'Une conscience pathogène est détectée. L’analyse révèle résistance adaptative, phases défensives et attaques coordonnées. Le combat final commence à 200.',es:'Se detecta una conciencia patógena. El análisis muestra resistencia adaptativa, fases defensivas y ataques coordinados. La batalla final comienza en 200.'}},
{id:8,wave:200,title:{ro:'08 — Ultimul protocol',it:'08 — L’Ultimo Protocollo',en:'08 — The Final Protocol',fr:'08 — Le Dernier Protocole',es:'08 — El Último Protocolo'},text:{ro:'OMEGA a intrat în nucleu. Nu poate fi învins prin forță brută: schimbă tipurile de noduri, controlează ferestrele de expunere, protejează nucleul și păstrează Overdrive pentru momentul critic.',it:'OMEGA è entrato nel nucleo. Non può essere sconfitto con la forza bruta: alterna i tipi di nodo, controlla le finestre di esposizione, proteggi il nucleo e conserva l’Overdrive per il momento critico.',en:'OMEGA has entered the core. Brute force will fail: rotate node types, control exposure windows, protect the core and save Overdrive for the critical moment.',fr:'OMEGA est entré dans le noyau. La force brute échouera : alternez les types de nœuds, contrôlez les fenêtres d’exposition, protégez le noyau et gardez l’Overdrive pour le moment critique.',es:'OMEGA ha entrado en el núcleo. La fuerza bruta fallará: alterna tipos de nodos, controla las ventanas de exposición, protege el núcleo y guarda Overdrive para el momento crítico.'}}
];


// ===================== MONETIZARE MEMRISTOR =====================
const MEMRISTOR_SMARTLINK = 'https://www.profitableratecpmnetwork.com/rcti3he9e?key=2dd1bb31766604425d3d90f70df44c8e';
function openMemristorSmartlink(){
  try {
    if (window.cordova && window.cordova.InAppBrowser) {
      window.cordova.InAppBrowser.open(MEMRISTOR_SMARTLINK, '_system');
      return true;
    }
    const w = window.open(MEMRISTOR_SMARTLINK, '_blank');
    if (!w) window.location.href = MEMRISTOR_SMARTLINK;
    return true;
  } catch(e) {
    try { window.location.href = MEMRISTOR_SMARTLINK; } catch(_) {}
    return false;
  }
}

// ===================== MAIN — navigare, UI, integrare Cordova =====================
const Main = (function () {
  let currentEndReason = null;

  // ---------- ecrane ----------
  function updateAdLabels(){
    const menu = document.getElementById('menuAdLabel');
    const more = document.getElementById('moreAdsLabel');
    const lang = (typeof SAVE !== 'undefined' && SAVE.language) ? SAVE.language : 'en';
    const labels = {ro:{ad:'PUBLICITATE',more:'Mai multe reclame'},it:{ad:'PUBBLICITÀ',more:'Altri annunci'},en:{ad:'ADVERTISEMENT',more:'More ads'},fr:{ad:'PUBLICITÉ',more:'Plus de publicités'},es:{ad:'PUBLICIDAD',more:'Más anuncios'}};
    const x = labels[lang] || labels.en;
    if(menu) menu.textContent = x.ad;
    if(more) more.textContent = x.more;
  }

  function goTo(id) {
    document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
    document.getElementById(id).classList.add('active');
    if (id === 'screen-game') requestAnimationFrame(() => { Game.resize(); const el=document.getElementById('v6CombatMap'); if(el&&Game.R&&MAPS[Game.R.mapId]) el.textContent=localizeMapName(MAPS[Game.R.mapId].name).replace(/^\S+ /,'').toUpperCase(); });
  }

  function toast(msg, ms) {
    const t = document.getElementById('globalToast');
    t.textContent = msg;
    t.classList.add('show');
    clearTimeout(t._timer);
    t._timer = setTimeout(() => t.classList.remove('show'), ms || 2200);
  }

  function showWaveToast(msg) {
    const t = document.getElementById('waveToast');
    t.textContent = msg;
    t.classList.add('show');
    clearTimeout(t._timer);
    t._timer = setTimeout(() => t.classList.remove('show'), 2600);
  }

  function applyLanguage(){
    CURRENT_LANG = detectPhoneLanguage();
    document.documentElement.lang = CURRENT_LANG;
    localizeCollections();
    // Static labels are applied to dedicated spans so icons and button structure remain intact.
    const labels={mapPickerLabel:'sector'};
    Object.keys(labels).forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=t(labels[id]);});
    const setText=(id,key)=>{const el=document.getElementById(id);if(el)el.textContent=ui(key);};
    const staticMap={
      'logoSub':'brandSub','towerDefenseLabel':'towerDefense','startDefenseLabel':'startDefense','continueLabel':'continue','tacticalMapLabel':'tacticalMap','arsenalLabel':'arsenal','rankingsLabel':'rankings','tacticsLabel':'tactics','chronicleLabel':'chronicle','systemLabel':'system',
      'howtoTitle':'howToPlay','shopTitle':'laboratory','settingsTitle':'settings','pauseTitle':'pause','nodeCloseBtn':'close','nodeFuseBtn':'fusion','nodeSellBtn':'sell','nodeUpgradeBtn':'upgrade','pauseSettingsBtn':'settings',
      'narrativeKicker':'archive','narrativeTitle':'implantJournal','btnContinueEndless':'continueEndless','btnRetry':'retry','btnEndMenu':'mainMenu','endBadgeNew':'newRecord','endTitle':'implantCompromised','waveButtonLabel':'waveButton'
    };
    Object.keys(staticMap).forEach(id=>setText(id,staticMap[id]));
    const setButton=(id,icon,key)=>{const el=document.getElementById(id);if(el)el.innerHTML=icon+' '+ui(key);};
    setButton('nodeUpgradeBtn','⬆','upgrade'); setButton('nodeFuseBtn','⚛️','fusion'); setButton('nodeSellBtn','💰','sell'); setButton('nodeCloseBtn','', 'close');
    setButton('pauseSettingsBtn','⚙','settings'); setButton('resumeBtn','▶','resume'); setButton('quitToMenuBtn','🚪','exitMenu'); setButton('narrativeClose','', 'continue');
    setButton('btnContinueEndless','♾','continueEndless'); setButton('btnRetry','↻','retry'); setButton('btnEndMenu','🏠','mainMenu');
    const endBadge=document.getElementById('endBadgeNew'); if(endBadge) endBadge.textContent='🏆 '+ui('newRecord');
    const combat=document.getElementById('combatLiveLabel'); if(combat) combat.textContent=ui('omegaProtocol')+' // '+ui('liveDefense');

    const setAttr=(id,attr,key)=>{const el=document.getElementById(id);if(el)el.setAttribute(attr,ui(key));};
    setAttr('memristorInAppAd','aria-label','advertisement');
    const textMap={
      splashText:'splash', defenseCommand:'defenseCommand', coreStatus:'coreStatus', bestWaveLabel:'bestWave', newCampaignLabel:'newCampaign', continueWaveLabel:'wave', uniqueSectorsLabel:'uniqueSectors', arsenalSub:'upgrades', rankingsSub:'achievements', tacticsSub:'howToPlay', chronicleSub:'chapters', systemSub:'settings', omegaProtocolFooter:'omegaProtocol',
      tacticalMapsTitle:'tacticalMaps', selectSectorTitle:'selectSector', mapsBadge:'maps', howObjective:'objective', howPlacement:'placement', howUpgradeSell:'upgradeSell', howWaves:'wavesTitle', howNano:'nanoTitle', howSpeed:'speedTitle',
      howObjectiveText:'objectiveText', howPlacementText:'placementText', howUpgradeSellText:'upgradeSellText', howWavesText:'wavesText', howNanoText:'nanoText', howSpeedText:'speedText',
      soundLabel:'sound', vibrateLabel:'vibrations', notifLabel:'newWaveNotifications', btnResetProgress:'resetProgress', settingsNote:'saveNote', integrityLabel:'integrity', combatLiveLabel:'liveDefense', nodeDefault:'node', pauseSettingsBtn:'settings',
      endWaveLabel:'waveReached', endKillsLabel:'threatsNeutralized', endScoreLabel:'score', endCrystalsLabel:'nanoWon'
    };
    Object.keys(textMap).forEach(id=>setText(id,textMap[id]));
    document.querySelectorAll('[data-i18n-key]').forEach(el=>el.textContent=ui(el.getAttribute('data-i18n-key')));
    renderStory();

    const adBtn = document.getElementById('btnRecommendations');
    if (adBtn) {
      const adLabels = {
        ro:'🌐 Recomandări', it:'🌐 Consigli', en:'🌐 Recommendations',
        fr:'🌐 Recommandations', es:'🌐 Recomendaciones'
      };
      adBtn.textContent = adLabels[CURRENT_LANG] || adLabels.en;
    }
  }

  // ---------- meniu principal ----------
  function renderMapPicker() {
    const sel=document.getElementById('mapSelect'); if(!sel) return;
    sel.innerHTML='';
    MAP_ORDER.forEach(id=>{ const m=MAPS[id]; const o=document.createElement('option'); o.value=id; o.textContent=localizeMapName(m.name); sel.appendChild(o); });
    sel.value=SAVE.selectedMap || 'neuralCore';
  }


  function renderDailyCard() {
    const card = document.getElementById('dailyCard'); if (!card) return;
    const c = getDailyChallenge();
    const best = SAVE.dailyBest[c.key] || 0;
    const played = SAVE.dailyLastPlayed === c.key;
    const streak = SAVE.dailyStreak || 0;
    card.innerHTML = `<div class="daily-head"><span>☀️ ${t('daily')}</span><b>${c.key}</b></div>
      <div class="daily-title">${c.icon} ${localizeMutation(c.mutation)} · ${localizeMapName(MAPS[c.mapId].name)}</div>
      <p>${localizeMutationDesc(c)}</p>
      <div class="daily-meta"><span>🎯 ${c.target} ${t('waves')}</span><span>💠 +${c.reward}</span><span>🔥 ${t('streak')} ${streak}</span><span>🏆 ${t('best')} ${best}</span></div>
      <button class="btn btn-daily" id="btnDailyRun">${played ? '↻ ' + t('daily') : '▶ ' + t('daily')}</button>`;
    document.getElementById('btnDailyRun').onclick = () => { AudioSys.buttonClick(); startDailyRun(); };
  }

  function loginDateKey(date=new Date()){
    const y=date.getFullYear(),m=String(date.getMonth()+1).padStart(2,'0'),d=String(date.getDate()).padStart(2,'0'); return `${y}-${m}-${d}`;
  }
  function claimDailyLogin(){
    const key=loginDateKey();
    if(SAVE.dailyLoginRewardsClaimed[key]) return;
    const prev=SAVE.dailyLoginLastClaim;
    const prevDate=prev?new Date(prev+'T00:00:00'):null;
    const today=new Date(key+'T00:00:00');
    const diff=prevDate?Math.round((today-prevDate)/86400000):0;
    let day=(diff===1?(SAVE.dailyLoginStreak||0)+1:1); if(day>7) day=1;
    const rewards=[10,15,20,25,30,45,75]; const reward=rewards[day-1];
    SAVE.dailyLoginStreak=day; SAVE.dailyLoginLastClaim=key; SAVE.dailyLoginRewardsClaimed[key]=true; SAVE.crystals+=reward; persistSave();
    toast(`🎁 ${t('loginReward')}: ${t('day')} ${day}/7 · +${reward}💠`,3200); renderDailyLogin(); refreshMenu();
  }
  function renderDailyLogin(){
    const card=document.getElementById('dailyLoginCard'); if(!card)return;
    const key=loginDateKey(), claimed=!!SAVE.dailyLoginRewardsClaimed[key];
    let preview=(SAVE.dailyLoginStreak||0)+1; if(preview>7)preview=1;
    const rewards=[10,15,20,25,30,45,75];
    card.innerHTML=`<div class="login-head"><span>🎁 ${t('dailyLogin')}</span><b>${t('day')} ${preview}/7</b></div><div class="login-streak">🔥 ${t('streak')} ${SAVE.dailyLoginStreak||0}/7</div><div class="login-rewards">${rewards.map((r,i)=>`<span class="${i+1===preview?'today':''} ${i+1<preview?'done':''}">${i+1}<small>💠${r}</small></span>`).join('')}</div><button class="btn ${claimed?'':'btn-login'}" id="btnClaimLogin" ${claimed?'disabled':''}>${claimed?'✓ '+t('claimed'):'🎁 '+t('claim')}</button>`;
    const b=document.getElementById('btnClaimLogin'); if(b&&!claimed)b.onclick=()=>claimDailyLogin();
  }
  function localizeMapName(name){
    if(CURRENT_LANG==='ro')return name;
    const k={'🧠 Nucleul Neural':['🧠 Neural Core','🧠 Noyau Neural','🧠 Núcleo Neural','🧠 Nucleo Neurale'],'🩸 Labirint Vascular':['🩸 Vascular Labyrinth','🩸 Labyrinthe Vasculaire','🩸 Laberinto Vascular','🩸 Labirinto Vascolare'],'🌿 Grădina Sinaptică':['🌿 Synaptic Garden','🌿 Jardin Synaptique','🌿 Jardín Sináptico','🌿 Giardino Sinaptico'],'⚛️ Membrana Cuantică':['⚛️ Quantum Membrane','⚛️ Membrane Quantique','⚛️ Membrana Cuántica','⚛️ Membrana Quantistica'],'🌀 Spirala Corticală':['🌀 Cortical Spiral','🌀 Spirale Corticale','🌀 Espiral Cortical','🌀 Spirale Corticale'],'🔥 Forja Sinaptică':['🔥 Synaptic Forge','🔥 Forge Synaptique','🔥 Forja Sináptica','🔥 Forgia Sinaptica'],'🧬 Labirint Limfatic':['🧬 Lymphatic Maze','🧬 Labyrinthe Lymphatique','🧬 Laberinto Linfático','🧬 Labirinto Linfatico'],'🏛️ Catedrala Axonilor':['🏛️ Axon Cathedral','🏛️ Cathédrale des Axones','🏛️ Catedral de Axones','🏛️ Cattedrale degli Assoni'],'☠️ Citadela Omega':['☠️ Omega Citadel','☠️ Citadelle Omega','☠️ Ciudadela Omega','☠️ Cittadella Omega']};
    const a=k[name]; if(!a)return name; return a[{it:0,en:0,fr:1,es:2}[CURRENT_LANG] ?? 3] || name;
  }
  function localizeMutation(x){ const byId=LOCALIZED_MUTATIONS[x]; if(byId){const v=byId[CURRENT_LANG]||byId.en; return v[0];} const m={'Overclock':{it:'Overclock',en:'Overclock',fr:'Overclock',es:'Overclock'},'Cryo Leak':{it:'Fuga Criogenica',en:'Cryo Leak',fr:'Fuite Cryo',es:'Fuga Criogénica'},'Swarm Bloom':{it:'Fioritura dello Sciame',en:'Swarm Bloom',fr:'Floraison d’Essaim',es:'Floración de Enjambre'},'Ranged Uprising':{it:'Rivolta a Distanza',en:'Ranged Uprising',fr:'Soulèvement à Distance',es:'Revuelta a Distancia'},'Low Energy':{it:'Energia Ridotta',en:'Low Energy',fr:'Énergie Faible',es:'Energía Baja'},'Glass Core':{it:'Nucleo di Vetro',en:'Glass Core',fr:'Noyau de Verre',es:'Núcleo de Cristal'}}; const d=m[x]; return d?(d[CURRENT_LANG]||x):x; }
  function localizeMutationDesc(c){ const v=LOCALIZED_MUTATIONS[c.mutationId]; return v?((v[CURRENT_LANG]||v.en)[1]):c.desc; }
  function renderStory(){
    const body=document.getElementById('storyBody'); if(!body)return;
    const chapters=window.MEMRISTOR_CHAPTERS||[];
    body.innerHTML=chapters.map((c,i)=>{const unlocked=!!SAVE.storyUnlocked[c.id];return `<article class="story-chapter ${unlocked?'unlocked':'locked'}"><span>${String(i+1).padStart(2,'0')}</span><h3>${c.title[CURRENT_LANG]||c.title.en}</h3><p>${unlocked?(c.text[CURRENT_LANG]||c.text.en):`🔒 ${t('locked')} ${c.wave}`}</p></article>`;}).join('');
  }


  function renderV6MapGrid(){
    const grid=document.getElementById('v6MapGrid'); if(!grid || typeof MAP_ORDER==='undefined') return;
    grid.innerHTML=MAP_ORDER.map((id,i)=>{
      const m=MAPS[id]; const unlocked=i===0 || (SAVE.bestWave||0)>=Math.max(1,i*15);
      const imgKey=['neural','vascular','synapse','quantum','cortical','forge','lymph','axon','omega'][i]||'neural';
      return `<button class="v6-map-card ${unlocked?'':'locked'}" data-map="${id}"><img src="img/v6-map-${imgKey}.svg"><div class="v6-map-meta"><span>${String(i+1).padStart(2,'0')}</span><b>${localizeMapName(m.name).replace(/^\S+ /,'')}</b><small>${unlocked?ui('online'):ui('locked')+' · '+ui('waveShort')+' '+(i*15+1)}</small></div></button>`;
    }).join('');
    grid.querySelectorAll('.v6-map-card').forEach(btn=>btn.onclick=()=>{
      if(btn.classList.contains('locked')){ toast(ui('lockedSector')); return; }
      SAVE.selectedMap=btn.dataset.map; persistSave(); AudioSys.buttonClick(); newGame();
    });
  }

  function refreshMenu() {
    updateAdLabels();
    document.getElementById('statBestWave').textContent = SAVE.bestWave;
    document.getElementById('statCrystals').textContent = SAVE.crystals;
    renderMapPicker();
    renderDailyCard();
    renderDailyLogin();
    renderStory();
    renderV6MapGrid();
    const bestBar=document.getElementById('v6BestBar'); if(bestBar) bestBar.style.width=Math.min(100,(SAVE.bestWave||0)/200*100)+'%';
    const contBtn = document.getElementById('btnContinue');
    if (SAVE.hasSavedRun && SAVE.savedRun) {
      contBtn.style.display = '';
      document.getElementById('continueWave').textContent = SAVE.savedRun.waveNumber;
    } else {
      contBtn.style.display = 'none';
    }
  }

  // ---------- HUD joc ----------
  function refreshHud() {
    const R = Game.R;
    if (!R) return;
    document.getElementById('hudEnergy').textContent = Math.floor(R.energy);
    document.getElementById('hudCrystals').textContent = SAVE.crystals;
    const waveLabel = R.waveNumber > MAX_DESIGNED_WAVE
      ? `Endless ${R.waveNumber - MAX_DESIGNED_WAVE}`
      : `${t('wave')} ${R.waveNumber}/${MAX_DESIGNED_WAVE}`;
    document.getElementById('hudWaveLabel').textContent = waveLabel;
    const pct = Math.max(0, Math.min(100, (R.coreHP / R.coreMaxHP) * 100));
    document.getElementById('hudCoreFill').style.width = pct + '%';
    const waveBtn = document.getElementById('btnStartWave');
    waveBtn.classList.toggle('hidden', R.waveInProgress);
    const od = Math.floor(R.overdrive || 0);
    const combo = R.combo || 0;
    const comboEl = document.getElementById('hudCombo'); if (comboEl) comboEl.textContent = ui('combo') + ' x' + combo;
    const odEl = document.getElementById('hudOverdrive'); if (odEl) odEl.textContent = (R.overdriveActive > 0 ? ui('active') : od + '%');
    const odFill = document.getElementById('hudOverdriveFill'); if (odFill) odFill.style.width = (R.overdriveActive > 0 ? 100 : od) + '%';
    const odBtn = document.getElementById('btnOverdrive'); if (odBtn) odBtn.classList.toggle('ready', od >= 100 && R.overdriveActive <= 0);
    renderToolbar();
  }

  // ---------- toolbar noduri ----------
  function renderToolbar() {
    const R = Game.R;
    if (!R) return;
    const bar = document.getElementById('nodeToolbar');
    bar.innerHTML = '';
    NODE_ORDER.forEach(typeId => {
      const def = NODE_TYPES[typeId];
      const cost = Game.nodeCost(typeId, 1);
      const btn = document.createElement('div');
      btn.className = 'node-btn' + (R.selected === typeId ? ' selected' : '') + (R.energy < cost ? ' disabled' : '');
      btn.innerHTML = `<div class="nb-ic">${def.icon}</div><div class="nb-cost">⚡${cost}</div><div class="nb-name">${def.name}</div>`;
      btn.onclick = () => {
        AudioSys.buttonClick();
        R.selected = (R.selected === typeId) ? null : typeId;
        renderToolbar();
      };
      bar.appendChild(btn);
    });
  }

  // ---------- popup nod ----------
  let popupNode = null;
  function openNodePopup(node) {
    popupNode = node;
    const def = NODE_TYPES[node.typeId];
    document.getElementById('nodePopupName').textContent = `${def.icon} ${def.name} · ${ui('level')}${node.level}`;
    document.getElementById('nodePopupDesc').textContent = def.desc;
    const statsEl = document.getElementById('nodePopupStats');
    const dmgLvl = Math.round(def.damage * (1 + (node.level - 1) * 0.3));
    const rangeLvl = Math.round(def.range * (1 + (node.level - 1) * 0.12));
    statsEl.innerHTML = def.id === 'amp'
      ? `<span>${ui('buff')} +${Math.round(def.buff * 100)}%</span><span>${ui('range')} ${rangeLvl}</span>`
      : `<span>${ui('damage')} ${dmgLvl}</span><span>${ui('range')} ${rangeLvl}</span><span>${ui('rate')} ${def.rate}s</span>`;
    const upBtn = document.getElementById('nodeUpgradeBtn');
    if (node.level >= 5) {
      upBtn.textContent = ui('maxLevel');
      upBtn.disabled = true;
    } else {
      upBtn.disabled = false;
      upBtn.textContent = `⬆ ${ui('upgrade')} (⚡${Game.nodeCost(node.typeId, node.level + 1)})`;
    }
    document.getElementById('nodeSellBtn').textContent = `💰 ${ui('sell')} (+⚡${Game.nodeSellValue(node)})`;
    const fuseBtn = document.getElementById('nodeFuseBtn');
    const partner = Game.nearbyFusionPartner ? Game.nearbyFusionPartner(node) : null;
    fuseBtn.disabled = !partner;
    fuseBtn.textContent = partner ? `⚛️ ${ui('fusionWith')} ${NODE_TYPES[partner.typeId].name}` : `⚛️ ${ui('fusionUnavailable')}`;
    const popup = document.getElementById('nodePopup');
    popup.classList.add('active');
    popup.setAttribute('aria-hidden', 'false');
  }
  function closeNodePopup() {
    popupNode = null;
    const popup = document.getElementById('nodePopup');
    if (popup) {
      popup.classList.remove('active');
      popup.setAttribute('aria-hidden', 'true');
    }
  }

  // ---------- toast la construcție ----------
  function onNodeBuilt() {
    Game.R.totalNodesBuilt = (Game.R.totalNodesBuilt || 0) + 1;
    renderToolbar();
  }

  // ---------- realizări ----------
  function combinedStats() {
    const R = Game.R || {};
    return {
      bestWave: SAVE.bestWave,
      totalKills: SAVE.totalKills,
      totalBossKills: SAVE.totalBossKills,
      totalRuns: SAVE.totalRuns,
      totalNodesBuilt: R.totalNodesBuilt || 0,
      laserBuilt: R.laserBuilt || 0,
      maxEndlessWave: R.maxEndlessWave || 0,
      totalOverdrives: SAVE.totalOverdrives || 0,
      maxEmpInRun: R.maxEmpInRun || 0,
      perfectWaveAchieved: !!R.perfectWaveAchieved,
      maxEnergyInRun: R.maxEnergyInRun || 0,
      allNodeTypesUsed: !!R.allNodeTypesUsed
    };
  }

  function checkAchievements() {
    const s = combinedStats();
    let unlockedAny = false;
    ACHIEVEMENTS.forEach(a => {
      if (SAVE.achievementsUnlocked[a.id]) return;
      if (a.check(s)) {
        SAVE.achievementsUnlocked[a.id] = true;
        SAVE.crystals += a.reward;
        unlockedAny = true;
        toast(`🏆 Realizare: ${a.name} (+${a.reward}💠)`, 2800);
      }
    });
    if (unlockedAny) persistSave();
  }

  function renderAchievements() {
    const list = document.getElementById('achList');
    list.innerHTML = '';
    let unlockedCount = 0;
    ACHIEVEMENTS.forEach(a => {
      const unlocked = !!SAVE.achievementsUnlocked[a.id];
      if (unlocked) unlockedCount++;
      const el = document.createElement('div');
      el.className = 'ach-item' + (unlocked ? ' unlocked' : '');
      el.innerHTML = `<span class="ach-ic">${unlocked ? a.icon : '🔒'}</span>
        <div class="ach-info"><b>${a.name}</b><p>${a.desc}</p></div>
        <span class="ach-reward">${unlocked ? '✓' : '+' + a.reward + '💠'}</span>`;
      list.appendChild(el);
    });
    document.getElementById('achProgress').textContent = `${unlockedCount}/${ACHIEVEMENTS.length}`;
  }

  // ---------- laborator (shop) ----------
  function renderShop() {
    document.getElementById('shopCrystals').textContent = SAVE.crystals;
    const list = document.getElementById('shopList');
    list.innerHTML = '';
    SHOP_ITEMS.forEach(item => {
      const lvl = shopLevel(item.id);
      const maxed = lvl >= item.maxLevel;
      const cost = maxed ? 0 : shopItemCost(item, lvl);
      const el = document.createElement('div');
      el.className = 'shop-item';
      el.innerHTML = `<span class="shop-ic">${item.icon}</span>
        <div class="shop-info"><b>${item.name}</b><p>${item.desc}</p><span class="lvl">${ui('level')} ${lvl}/${item.maxLevel}</span></div>
        <button class="shop-buy ${maxed ? 'maxed' : ''}" ${maxed || SAVE.crystals < cost ? 'disabled' : ''}>${maxed ? 'MAX' : '💠 ' + cost}</button>`;
      el.querySelector('button').onclick = () => {
        if (maxed || SAVE.crystals < cost) return;
        SAVE.crystals -= cost;
        SAVE.shopLevels[item.id] = lvl + 1;
        persistSave();
        AudioSys.upgrade();
        renderShop();
        refreshMenu();
      };
      list.appendChild(el);
    });
  }

  // ---------- setări ----------
  function initSettingsUI() {
    const sSound = document.getElementById('toggleSound');
    const sVibr = document.getElementById('toggleVibrate');
    const sNotif = document.getElementById('toggleNotif');
    sSound.checked = SAVE.settings.sound;
    sVibr.checked = SAVE.settings.vibrate;
    sNotif.checked = SAVE.settings.notif;
    AudioSys.setEnabled(SAVE.settings.sound);

    sSound.onchange = () => { SAVE.settings.sound = sSound.checked; AudioSys.setEnabled(sSound.checked); persistSave(); };
    sVibr.onchange = () => { SAVE.settings.vibrate = sVibr.checked; persistSave(); };
    sNotif.onchange = () => { SAVE.settings.notif = sNotif.checked; persistSave(); };

    document.getElementById('btnResetProgress').onclick = () => {
      confirmDialog(ui('resetConfirm'), ok => {
        if (ok) {
          resetAllProgress();
          loadSave();
          initSettingsUI();
          refreshMenu();
          toast(ui('progressReset'));
        }
      });
    };
  }

  function confirmDialog(msg, cb) {
    if (window.navigator && navigator.notification && navigator.notification.confirm) {
      navigator.notification.confirm(msg, idx => cb(idx === 1), ui('confirmTitle'), [ui('yes'), ui('no')]);
    } else {
      cb(window.confirm(msg));
    }
  }

  // ---------- ciclu rulare (run) ----------

  function startDailyRun() {
    const c = getDailyChallenge();
    const prev = SAVE.dailyLastPlayed;
    const prevDate = prev ? new Date(prev + 'T00:00:00Z') : null;
    const today = new Date(c.key + 'T00:00:00Z');
    const dayDiff = prevDate ? Math.round((today - prevDate) / 86400000) : 0;
    SAVE.dailyStreak = dayDiff === 1 ? (SAVE.dailyStreak || 0) + 1 : (prev === c.key ? (SAVE.dailyStreak || 0) : 1);
    SAVE.dailyLastPlayed = c.key;
    persistSave();
    SAVE.hasSavedRun = false; SAVE.savedRun = null; persistSave();
    Game.startDailyRun();
    goTo('screen-game'); refreshHud();
  }

  function newGame() {
    SAVE.hasSavedRun = false;
    SAVE.savedRun = null;
    persistSave();
    Game.setMap(SAVE.selectedMap || 'neuralCore');
    Game.startNewRun();
    goTo('screen-game');
    refreshHud();
  }

  function continueGame() {
    const s=SAVE.savedRun; if(!s) return newGame();
    const preset={...s, nodes:(s.nodes||[]).map(n=>({...n})), enemies:(s.enemies||[]).map(e=>({...e})), projectiles:(s.projectiles||[]).map(p=>({...p})), enemyProjectiles:(s.enemyProjectiles||[]).map(p=>({...p})), particles:(s.particles||[]).map(p=>({...p})), anomalies:(s.anomalies||[]).map(a=>({...a})), shockwaves:(s.shockwaves||[]).map(a=>({...a})), beams:(s.beams||[]).map(a=>({...a})), paused:false, running:true, finalized:false };
    Game.startNewRun(preset); goTo('screen-game'); refreshHud();
  }

  function autoSaveRun() {
    const R=Game.R; if(!R) return; const snap=Game.getSnapshot ? Game.getSnapshot() : null;
    SAVE.hasSavedRun=true;
    SAVE.savedRun=snap || {energy:R.energy,coreHP:R.coreHP,coreMaxHP:R.coreMaxHP,waveNumber:R.waveNumber,mapId:R.mapId,nodes:R.nodes.map(n=>({slot:n.slot,typeId:n.typeId,level:n.level}))};
    SAVE.selectedMap=R.mapId || SAVE.selectedMap; persistSave();
  }

  function showNarrativeBeat(wave,mapId){
    const chapters=window.MEMRISTOR_CHAPTERS||[]; const c=chapters.find(x=>x.wave===wave); if(!c)return;
    if(SAVE.storyUnlocked[c.id]) return;
    SAVE.storyUnlocked[c.id]=true; persistSave(); renderStory();
    const ov=document.getElementById('narrativeOverlay'); if(!ov)return;
    document.getElementById('narrativeKicker').textContent=`${String(c.id).padStart(2,'0')} · ${localizeMapName(MAPS[mapId]?.name||'SECTOR')}`;
    document.getElementById('narrativeTitle').textContent=c.title[CURRENT_LANG]||c.title.en;
    document.getElementById('narrativeText').textContent=c.text[CURRENT_LANG]||c.text.en;
    ov.classList.add('active');
    if(Game.R) Game.R.paused=true;
  }

  function showVictory() {
    currentEndReason = 'victory';
    const R = Game.R;
    document.getElementById('endTitle').textContent = (R.waveNumber - 1 >= FINAL_BOSS_WAVE) ? t('final') : ui('victoryStable');
    document.getElementById('endTitle').classList.add('victory');
    document.getElementById('endWave').textContent = R.waveNumber - 1;
    document.getElementById('endKills').textContent = R.killsThisRun;
    document.getElementById('endScore').textContent = R.killsThisRun * 10 + (R.waveNumber - 1) * 50;
    document.getElementById('endCrystals').textContent = '+' + R.runCrystalsEarned;
    document.getElementById('endBadgeNew').style.display = 'none';
    document.getElementById('btnContinueEndless').style.display = (R.waveNumber - 1 >= FINAL_BOSS_WAVE) ? 'none' : '';
    AudioSys.victory();
    if (navigator.vibrate && SAVE.settings.vibrate) navigator.vibrate([0, 100, 60, 100, 60, 200]);
    SAVE.totalRuns = (SAVE.totalRuns || 0) + 1;
    SAVE.hasSavedRun = false;
    SAVE.savedRun = null;
    persistSave();
    refreshMenu();
    goTo('screen-end');
  }

  function onRunEnded(reason) {
    if (reason !== 'gameover') return; // victoria e gestionată separat de showVictory()
    currentEndReason = 'gameover';
    const R = Game.R;
    document.getElementById('endTitle').textContent = ui('implantCompromised');
    document.getElementById('endTitle').classList.remove('victory');
    const waveReached = Math.max(0, R.waveNumber - (R.waveInProgress ? 0 : 0));
    document.getElementById('endWave').textContent = R.waveNumber;
    document.getElementById('endKills').textContent = R.killsThisRun;
    document.getElementById('endScore').textContent = R.killsThisRun * 10 + R.waveNumber * 50;
    document.getElementById('endCrystals').textContent = '+' + R.runCrystalsEarned;
    document.getElementById('btnContinueEndless').style.display = 'none';
    document.getElementById('endBadgeNew').style.display = (R.waveNumber >= SAVE.bestWave && R.waveNumber > 0) ? '' : 'none';
    refreshMenu();
    goTo('screen-end');
  }

  // ---------- wiring evenimente UI ----------
  function wireUI() {
    document.querySelectorAll('.back-btn').forEach(b => {
      b.onclick = () => { AudioSys.buttonClick(); goTo(b.dataset.back); refreshMenu(); };
    });

    document.getElementById('btnNewGame').onclick = () => { AudioSys.buttonClick(); newGame(); };
    document.getElementById('btnMapSelect').onclick = () => { AudioSys.buttonClick(); renderV6MapGrid(); goTo('screen-map'); };
    document.getElementById('btnContinue').onclick = () => { AudioSys.buttonClick(); continueGame(); };
    document.getElementById('btnShop').onclick = () => { AudioSys.buttonClick(); renderShop(); goTo('screen-shop'); };
    document.getElementById('btnAchievements').onclick = () => { AudioSys.buttonClick(); renderAchievements(); goTo('screen-achievements'); };
    document.getElementById('btnHowTo').onclick = () => { AudioSys.buttonClick(); goTo('screen-howto'); };
    document.getElementById('btnStory').onclick = () => { AudioSys.buttonClick(); goTo('screen-story'); };
    const mapSel=document.getElementById('mapSelect'); if(mapSel) mapSel.onchange=()=>{ SAVE.selectedMap=mapSel.value; persistSave(); };
    document.getElementById('narrativeClose').onclick=()=>{document.getElementById('narrativeOverlay').classList.remove('active'); if(Game.R&&Game.R.running)Game.R.paused=false;};
    document.getElementById('btnSettings').onclick = () => { AudioSys.buttonClick(); goTo('screen-settings'); };
    const recBtn = document.getElementById('btnRecommendations'); if(recBtn) recBtn.onclick = () => { AudioSys.buttonClick(); openMemristorSmartlink(); };

    document.getElementById('btnStartWave').onclick = () => { AudioSys.buttonClick(); Game.startWave(); refreshHud(); };
    document.getElementById('btnOverdrive').onclick = () => { if (!Game.activateOverdrive()) toast(ui('overdriveNeed')); refreshHud(); };
    document.getElementById('btnPause').onclick = () => {
      AudioSys.buttonClick();
      Game.R.paused = true;
      autoSaveRun();
      document.getElementById('pauseOverlay').classList.add('active');
    };
    document.getElementById('resumeBtn').onclick = () => {
      AudioSys.buttonClick();
      Game.R.paused = false;
      document.getElementById('pauseOverlay').classList.remove('active');
    };
    document.getElementById('pauseSettingsBtn').onclick = () => { AudioSys.buttonClick(); goTo('screen-settings'); };
    document.getElementById('quitToMenuBtn').onclick = () => {
      AudioSys.buttonClick();
      document.getElementById('pauseOverlay').classList.remove('active');
      Game.endRun('quit');
      goTo('screen-menu');
      refreshMenu();
    };

    document.getElementById('btnSpeed').onclick = () => {
      AudioSys.buttonClick();
      const seq = [1, 2, 3];
      const idx = seq.indexOf(Game.R.speedMult);
      Game.R.speedMult = seq[(idx + 1) % seq.length];
      document.getElementById('btnSpeed').textContent = Game.R.speedMult + 'x';
    };

    // ===== V6.0.3 UNIVERSAL MOBILE BUTTON BRIDGE =====
    // Some Android WebView/device combinations can swallow the synthetic click
    // after a touch gesture. Capture touchend at document level and dispatch the
    // element's existing click handler exactly once. This covers every real
    // <button> and the dynamically-created .node-btn controls.
    if (!window.__memristorUniversalTouchBridge) {
      window.__memristorUniversalTouchBridge = true;
      let lastTouchEl = null;
      let lastTouchAt = 0;
      const fireButton = (el, ev) => {
        if (!el || el.disabled) return;
        const now = Date.now();
        if (lastTouchEl === el && now - lastTouchAt < 550) return;
        lastTouchEl = el; lastTouchAt = now;
        try { ev.preventDefault(); ev.stopPropagation(); } catch(e) {}
        try { el.click(); } catch(e) {
          try { if (typeof el.onclick === 'function') el.onclick.call(el, ev); } catch(ignore) {}
        }
      };
      document.addEventListener('touchend', ev => {
        const target = ev.target && ev.target.closest ? ev.target.closest('button,.node-btn') : null;
        if (target) fireButton(target, ev);
      }, {capture:true, passive:false});
      document.addEventListener('pointerup', ev => {
        if (ev.pointerType === 'touch') return;
        const target = ev.target && ev.target.closest ? ev.target.closest('button,.node-btn') : null;
        if (target) fireButton(target, ev);
      }, {capture:true, passive:false});
    }

    // ===== V6.0.1 ROBUST TOUCH CONTROLS =====
    // Android WebView/touch fallback: execute the action from touchend and suppress
    // the synthetic click that may follow it. Audio errors are never allowed to
    // block the actual game action.
    function safeButtonSound(){ try { AudioSys.buttonClick(); } catch(e) {} }
    function bindReliableButton(id, action){
      const el = document.getElementById(id);
      if (!el) return;
      let lastTouch = 0;
      el.onclick = (ev) => {
        if (Date.now() - lastTouch < 650) return;
        try { action(ev); } catch(e) { console.error('[MEMRISTOR BUTTON]', id, e); }
      };
      // Touch is handled by the universal capture bridge above. Keep a direct
      // fallback for environments that do not expose the bridge.
      el.ontouchend = (ev) => {
        lastTouch = Date.now();
        try { ev.preventDefault(); ev.stopPropagation(); } catch(e) {}
        try { action(ev); } catch(e) { console.error('[MEMRISTOR TOUCH]', id, e); }
      };
    }

    bindReliableButton('nodeCloseBtn', () => {
      safeButtonSound();
      closeNodePopup();
    });

    bindReliableButton('nodeUpgradeBtn', () => {
      if (!popupNode) return;
      safeButtonSound();
      try {
        const upgraded = Game.tryUpgradeNode(popupNode);
        if (upgraded && popupNode) openNodePopup(popupNode);
        else if (popupNode) openNodePopup(popupNode);
      } catch(e) {
        console.error('[MEMRISTOR UPGRADE]', e);
      }
    });

    bindReliableButton('nodeSellBtn', () => {
      if (!popupNode) return;
      safeButtonSound();
      try { Game.sellNode(popupNode); } catch(e) { console.error('[MEMRISTOR SELL]', e); }
      closeNodePopup();
      renderToolbar();
      refreshHud();
    });

    bindReliableButton('nodeFuseBtn', () => {
      if (!popupNode) return;
      safeButtonSound();
      try {
        if (Game.tryFuseNode(popupNode)) { closeNodePopup(); renderToolbar(); refreshHud(); }
        else openNodePopup(popupNode);
      } catch(e) {
        console.error('[MEMRISTOR FUSION]', e);
      }
    });

    const nodePopupEl = document.getElementById('nodePopup');
    if (nodePopupEl) {
      nodePopupEl.setAttribute('aria-hidden', 'true');
      nodePopupEl.addEventListener('touchstart', ev => {
        if (ev.target === nodePopupEl) { try { ev.preventDefault(); } catch(e) {} }
      }, {passive:false});
      nodePopupEl.addEventListener('click', ev => {
        if (ev.target === nodePopupEl) closeNodePopup();
      });
    }

    document.getElementById('btnRetry').onclick = () => { AudioSys.buttonClick(); newGame(); };
    document.getElementById('btnEndMenu').onclick = () => {
      AudioSys.buttonClick();
      if (currentEndReason === 'victory') Game.endRun('quit');
      goTo('screen-menu');
      refreshMenu();
    };
    document.getElementById('btnContinueEndless').onclick = () => {
      AudioSys.buttonClick();
      Game.R.endlessMode = true;
      Game.R.running = true;
      goTo('screen-game');
      refreshHud();
    };
  }

  // ---------- init sigur ----------
  // V5.4.1 BOOTFIX: pornirea jocului NU mai depinde de deviceready.
  // Cordova poate întârzia acest eveniment sau un plugin poate eșua; meniul trebuie
  // să poată porni oricum. Inițializarea nativă rămâne separată și opțională.
  let bootStarted = false;
  let deviceReady = false;

  function safeMenuFallback(error) {
    try {
      const fill = document.getElementById('loadbarFill');
      if (fill) fill.style.width = '100%';
      const splash = document.getElementById('screen-splash');
      const menu = document.getElementById('screen-menu');
      if (splash) splash.classList.remove('active');
      if (menu) menu.classList.add('active');
    } catch (e) {}
    // Nu blocăm aplicația din cauza unui modul opțional. Eroarea rămâne disponibilă
    // în consola WebView pentru diagnosticare.
    try { console.error('[MEMRISTOR BOOT]', error); } catch (e) {}
  }

  function boot() {
    if (bootStarted) return;
    bootStarted = true;

    const fill = document.getElementById('loadbarFill');
    if (fill) fill.style.width = '8%';

    try { loadSave(); } catch (e) { SAVE = defaultSave(); }
    if (fill) fill.style.width = '22%';

    try { applyLanguage(); } catch (e) {
      try { CURRENT_LANG = 'ro'; localizeNodeData(); } catch (ignore) {}
      console.error('[MEMRISTOR LANGUAGE]', e);
    }
    if (fill) fill.style.width = '38%';

    try {
      const canvas = document.getElementById('gameCanvas');
      if (canvas) Game.init(canvas);
    } catch (e) {
      console.error('[MEMRISTOR GAME INIT]', e);
    }
    if (fill) fill.style.width = '55%';

    try { wireUI(); } catch (e) { console.error('[MEMRISTOR UI]', e); }
    if (fill) fill.style.width = '70%';

    try { initSettingsUI(); } catch (e) { console.error('[MEMRISTOR SETTINGS]', e); }
    if (fill) fill.style.width = '82%';

    try { refreshMenu(); } catch (e) { console.error('[MEMRISTOR MENU]', e); }
    if (fill) fill.style.width = '100%';

    // Nu mai lăsăm splash-ul blocat dacă un pas opțional a eșuat.
    setTimeout(() => {
      try { goTo('screen-menu'); }
      catch (e) { safeMenuFallback(e); }
    }, 180);

    // Deblochează AudioContext la prima interacțiune (politică browser/mobil).
    const unlock = () => {
      try { AudioSys.unlock(); } catch (e) {}
    };
    document.addEventListener('touchstart', unlock, { once: true, passive: true });
    document.addEventListener('click', unlock, { once: true });
  }

  // ---------- integrare Cordova ----------
  function onDeviceReady() {
    if (deviceReady) return;
    deviceReady = true;
    try {
      if (window.StatusBar) {
        StatusBar.styleLightContent();
        StatusBar.backgroundColorByHexString('#0B1020');
      }
    } catch (e) {}

    if (!window.__memristorLifecycleWired) {
      window.__memristorLifecycleWired = true;
      document.addEventListener('pause', () => {
        if(Game.R){ Game.R.paused=true; autoSaveRun(); }
        try {
          if (SAVE && SAVE.settings.notif && window.cordova && cordova.plugins && cordova.plugins.notification) {
            cordova.plugins.notification.local.schedule({
              id: 1,
              title: 'Memristor: Apărarea Implantului',
              text: 'Circuitul așteaptă comenzi. Revino pentru valul următor!',
              trigger: { in: 3, unit: 'hour' }
            });
          }
        } catch (e) {}
      }, false);
      document.addEventListener('resume', () => {
        try {
          if (window.cordova && cordova.plugins && cordova.plugins.notification) {
            cordova.plugins.notification.local.cancel(1);
          }
        } catch (e) {}
      }, false);
    }
  }

  document.addEventListener('deviceready', onDeviceReady, false);
  // Pornire garantată după DOM. deviceready rămâne doar pentru funcțiile native.
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', boot, { once: true });
  } else {
    boot();
  }

  return {
    onNodeBuilt, showWaveToast, showNarrativeBeat, checkAchievements, refreshHud, autoSaveRun,
    openNodePopup, onRunEnded, showVictory
  };
})();
